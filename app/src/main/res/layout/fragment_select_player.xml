<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:orientation="vertical"
    android:onClick="eatClickEvent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".campaign.CampaignOverviewActivity">

    <include
        android:id="@+id/fragment_toolbar"
        layout="@layout/fragment_toolbar" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:minHeight="56dp"

        android:padding="8dp"
        android:text="@string/add_pc_info_text"
        android:textSize="8pt" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_margin="4dp"
        android:background="@drawable/round_corner"

        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:onClick="addPlayerCampaignButton"

        android:padding="12dp"
        android:text="Add New"
        android:textSize="8pt"
        tools:ignore="OnClick" />


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:minHeight="56dp"

        android:padding="8dp"
        android:text="@string/select_pc_info_text"
        android:textSize="8pt" />

    <Spinner
        android:id="@+id/player_spinner"

        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_margin="4dp"
        android:background="@drawable/round_corner"

        android:gravity="center"

        android:padding="12dp"
        android:spinnerMode="dialog"

        android:textSize="8pt" />

</LinearLayout>